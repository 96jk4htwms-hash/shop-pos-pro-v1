# รายงานตรวจสอบและการแก้ไข — Smart POS Pro V2 → V3

## วิธีติดตั้ง (ทำตามลำดับ)
1. **สำรองฐานข้อมูลก่อน** (Supabase → Database → Backups) และกด "ดาวน์โหลดไฟล์สำรอง" ในแอปเดิม
2. เปิด Supabase → SQL Editor → รัน `pos_patch_v3.sql` (รันซ้ำได้ปลอดภัย) — ถ้าสร้างฐานข้อมูลใหม่ทั้งหมดให้รัน `pos_schema_v3.sql` ไฟล์เดียว
3. นำโฟลเดอร์ **`pos-app-pwa/` ทั้งโฟลเดอร์** ไปวางบนเว็บที่เป็น **https** (เช่น Netlify / Cloudflare Pages / GitHub Pages / Vercel — ลากโฟลเดอร์ไปวางได้เลย) แล้วเปิดลิงก์บนมือถือ → กด "ติดตั้งแอป" (Android) หรือ แชร์ → "เพิ่มไปยังหน้าจอโฮม" (iPhone)
   - ต้องใช้ทั้ง 4 ส่วนพร้อมกัน: `index.html`, `sw.js`, `manifest.webmanifest`, `icons/` — ถ้าเปิด `index.html` ตรงๆ จากไฟล์ แอปยังใช้งานได้ แต่ติดตั้งเป็นแอป/ใช้ออฟไลน์ไม่ได้
   - ข้อมูลเดิมในเครื่อง (`pos2_db`) อ่านต่อได้ถ้าใช้โดเมนเดิม แต่ **เว็บคนละโดเมนจะเห็นข้อมูลในเครื่องคนละชุด** — ย้ายโดเมนให้ "ดาวน์โหลดไฟล์สำรอง" จากที่เก่าแล้ว "กู้คืน" ที่ใหม่
   - ปล่อยเวอร์ชันใหม่: แก้ `VERSION` ในบรรทัดแรกของ `sw.js` ทุกครั้ง แล้วอัปโหลดทับ → ผู้ใช้จะเห็นแถบ "มีเวอร์ชันใหม่ [อัปเดต]" (ไม่รีโหลดกลางการขายเอง)
4. ตั้ง PIN ให้เจ้าของร้านที่แท็บ **พนักงาน** ก่อนใช้ปุ่ม 🔒 ล็อก

> **ต้องรัน SQL ก่อนใช้งานจริง** ไม่งั้นปุ่มลบจะค้างในคิว "รอลบบนคลาวด์" และหน้าประวัติแบบ "คลาวด์" / ปุ่มล้างฐานข้อมูลจะใช้ไม่ได้

---

## 1) บั๊กที่พบในโค้ดเดิมและวิธีแก้

### ทำให้ข้อมูล/เงินผิด (ร้ายแรง)
| # | อาการเดิม | แก้แล้ว |
|---|---|---|
| 1 | ไม่มีระบบสิทธิ์เลย — แคชเชียร์เปิดแท็บพนักงานแก้ตำแหน่งตัวเองเป็น OWNER, ลบข้อมูลได้ทุกอย่าง | สิทธิ์ 13 ข้อ ตรวจทั้ง "การเข้าหน้า" และ "ทุกการกระทำ" + ทริกเกอร์ฝั่งเซิร์ฟเวอร์กันผู้จัดการแก้บัญชีเจ้าของ |
| 2 | ขายผ่านหน้าจอได้เฉพาะขนาดแรก (`variants[0]`) ขนาดอื่นต้องสแกนบาร์โค้ดเท่านั้น | กดสินค้าหลายขนาด → เลือกขนาด/หน่วยแบ่งขายได้ครบ |
| 3 | ขายเกินสต็อกได้ (ไม่นับของในตะกร้า/หน่วยเต็ม+หน่วยแบ่งขายของขนาดเดียวกัน) | เช็คยอดรวมทุกบรรทัดตอนเพิ่มของและตอนชำระเงิน |
| 4 | **บิลที่ขายตอนเปิดกะซิงค์ไม่ผ่านทุกใบ** — `bills.shift_id` อ้าง `shifts` แต่ไม่เคยส่งกะขึ้นไป (ชน FK) | ส่งกะขึ้นคลาวด์ก่อนบิลเสมอ |
| 5 | **คืนสินค้าไม่ได้เลย** — `create_sale` สร้าง id บิลเองบนเซิร์ฟเวอร์ แต่หน้าคืนสินค้าส่ง id ที่คำนวณจากเลขบิล | เก็บ `serverId` ที่เซิร์ฟเวอร์ตอบกลับ + ค้นหาสำรองด้วยเลขบิล |
| 6 | คืนสินค้าซ้ำเกินจำนวนที่ซื้อได้ / ช่องค้นหาว่างได้บิลแรกของระบบ | ติดตามยอดคืนสะสมรายรายการ, ต้องพิมพ์เลขบิลก่อน |
| 7 | **"ดึงข้อมูลจากคลาวด์" ทำให้อ้างอิงเดิมพังหมด** (บิล/ตะกร้า/ใบสั่งซื้อชี้ id เก่า) เพราะเอา uuid คลาวด์มาแทน id ในเครื่อง | จับคู่ด้วย id เดิมและคงไว้ |
| 8 | ดึงข้อมูลได้ไม่เกิน 1,000 แถว (ค่าเริ่มต้น PostgREST) แบบเงียบๆ | ดึงเป็นหน้าๆ จนหมด |
| 9 | ส่งบิลไม่สำเร็จแล้วเงียบ ไม่ลองใหม่ | คิวลองใหม่อัตโนมัติ (ถอยระยะ) + กันบิลซ้ำถ้าตอบกลับหาย |
| 10 | ข้อมูลใน localStorage เสีย → แอปเปิดไม่ขึ้นอีก / ไฟล์กู้คืนผิดประเภทก็ทำแอปพัง | อ่านแบบปลอดภัย เก็บสำเนาข้อมูลเสีย, ตรวจไฟล์กู้คืนก่อนเขียนทับ |
| 11 | ลบสินค้า/ขนาดในเครื่องแล้ว ดึงข้อมูลทีไรก็กลับมา (ไม่เคยลบบนคลาวด์) | ระบบลบใหม่ (ข้อ 2) |
| 12 | ชื่อสินค้า/ลูกค้าที่มี `<`, `"` ทำหน้าพังหรือรันสคริปต์ได้ (XSS) | `esc()` ทุกจุดที่แสดงผล |
| 13 | เปลี่ยนชื่อหมวดแล้วสินค้าหลุดจากหมวดเงียบๆ; เอาสินค้าออกจากหมวดแล้วดึงข้อมูลกลับมาอยู่หมวดเดิม | เปลี่ยนชื่อต่อเนื่องถึงสินค้า + ลบลิงก์เก่าบนคลาวด์ |
| 14 | แก้พนักงานที่ผูกคลาวด์แล้ว (ชื่อ/ตำแหน่ง/PIN) ไม่เคยขึ้นคลาวด์ (ฟังก์ชัน return ทิ้ง) | ส่งอัปเดตจริง |
| 15 | ใบสั่งซื้อที่ส่งขึ้นคลาวด์ไม่สำเร็จ รับของไม่ได้ตลอด; ขนาดซ้ำ 2 บรรทัด id ชนกัน | ส่งซ้ำก่อนรับ, รวมบรรทัดซ้ำ |
| 16 | เรียกบิลพักกลับมา → ทับตะกร้าปัจจุบันเงียบๆ | พักตะกร้าเดิมให้อัตโนมัติ |
| 17 | เก็บ `db.activeUser` เป็นสำเนา (ตำแหน่ง/PIN เก่าค้าง) | เก็บเป็น id แล้วค้นสดทุกครั้ง |
| 18 | ไม่มี toast — กดแล้ว "ไม่เกิดอะไรขึ้น" เพราะ error ไปโผล่แท็บ Log | แจ้งเตือนทันทีทุกจุดที่ผู้ใช้ทำ |

### ที่เจอเพิ่มระหว่างทดสอบ V3 (แก้แล้ว)
รหัสบิลพักซ้ำได้ในมิลลิวินาทีเดียวกัน • ตารางสินค้าเรียงผิดเมื่อชื่อต่างกันแค่เว้นวรรค • ประวัติการสร้างพนักงานคนแรกลงชื่อ "(ระบบ)" • เจ้าของตั้ง PIN ตัวเองเป็นคนแรกแล้วโดนล็อกออกจากทุกหน้า • ขนาดที่ถูกลบจากเครื่องอื่นอาจถูกปลุกคืนชีพตอนซิงค์

---

## 2) ระบบลบข้อมูล + ตรวจรายการซ้ำ

**ลบ (สินค้า / ขนาด / หมวดหมู่ / ลูกค้า / ซัพพลายเออร์ / ใบสั่งซื้อ / พนักงาน)**
- ลบในเครื่องทันที → ใส่คิว → เรียก `delete_entity` บนเซิร์ฟเวอร์ ถ้าออฟไลน์คิวค้างและลองใหม่เองทุก 60 วินาที/ตอนล็อกอิน (แถบเหลืองบนหน้าสินค้าแสดงจำนวนที่รอ)
- **มีบิล/ใบสั่งซื้อ/ประวัติสต็อกผูกอยู่ → "เก็บเข้าประวัติ" (ซ่อน) ไม่ลบถาวร** เพื่อไม่ให้บิลเก่าพัง และปล่อยรหัส SKU/บาร์โค้ดให้ใช้ใหม่ได้ · ไม่มี → ลบถาวรพร้อมรูปใน Storage
- ป้องกัน: ลบลูกค้าที่ยังมีหนี้ไม่ได้, พนักงานที่มีประวัติลบไม่ได้ (ให้ "ปิดใช้" แทน), ใบสั่งซื้อที่รับของแล้วลบไม่ได้
- ติ๊กเลือกหลายสินค้าแล้ว "ลบที่เลือก" ได้ · ก่อนลบทุกครั้งมีหน้าต่างยืนยันบอกผลกระทบ (สต็อกที่จะหาย, จำนวนบิลที่เคยขาย)

**ปุ่ม 🔍 ตรวจรายการซ้ำ** (แสดงจำนวนกลุ่มที่ซ้ำ) ตรวจ 4 แบบ: ชื่อซ้ำ (ไม่สนช่องว่าง/ตัวพิมพ์/เครื่องหมาย) · รหัส SKU ซ้ำ · บาร์โค้ดซ้ำ · ขนาดซ้ำในสินค้าเดียว — แต่ละรายการมีปุ่มแก้ไข/ลบ, กลุ่ม "ชื่อซ้ำ" มี **รวมสินค้า** (เก็บอันที่เลือก ลบที่เหลือ โอนสต็อกขนาดที่ตรงกันให้)
และกันซ้ำตอนบันทึก/นำเข้า Excel ด้วย (SKU, บาร์โค้ด, ขนาด, ชื่อ)

---

## 3) พนักงาน / รายงาน / ประวัติการใช้งาน
- **พนักงาน**: ตำแหน่ง เจ้าของ/ผู้จัดการ/แคชเชียร์ · แคชเชียร์ติ๊กสิทธิ์เพิ่มรายคนได้ · ปิด/เปิดใช้งาน · รีเซ็ต PIN · ดูวันเวลาเข้าใช้ล่าสุด จำนวนครั้ง ยอดขายวันนี้ จำนวนครั้งที่ใส่ PIN ผิดใน 24 ชม. · PIN ผิด 5 ครั้งล็อก 60 วินาที · ปฏิเสธ PIN เดาง่าย (0000, 1234) และชื่อซ้ำ
- **รายงาน**: เลือกช่วงวันที่/รายพนักงาน · ยอดขาย−คืน=สุทธิ · แยกวิธีชำระ/รายวัน/รายพนักงาน · สินค้าขายดี (หักของที่คืน) · รายการบิลย้อนหลังแตะดูรายละเอียด การคืน และเส้นทางเหตุการณ์ของบิลนั้น · ส่งออก Excel 4 ชีต
- **ประวัติการใช้งาน** (แท็บใหม่): บันทึกเข้า/ออก/ใส่ PIN ผิด, ขาย, คืน, เปิด-ปิดร้าน (เงินขาด/เกิน), แก้ราคา/ทุน (ค่าเก่า→ใหม่), ปรับสต็อก (ต้องระบุเหตุผล), ลบ/รวมสินค้า, เปลี่ยนสิทธิ์พนักงาน, การถูกปฏิเสธสิทธิ์ ฯลฯ · กรองตามผู้ใช้/ประเภท/วันที่/คำค้น · เลือกดูจาก "เครื่องนี้" หรือ "คลาวด์ (ทุกเครื่อง)" · ซิงค์ขึ้นตาราง `audit_logs` ที่ **แก้/ลบไม่ได้** (ทริกเกอร์กันแม้เป็นเจ้าของ)

---

## 4) ล้างข้อมูลเก่า / เริ่มระบบใหม่ (ตั้งค่า → 🧹 ล้างข้อมูลเก่า — เฉพาะเจ้าของร้าน)
**ล้างที่ไหน:** ① เฉพาะในเครื่องนี้ (ไม่แตะฐานข้อมูล) ② ในเครื่องนี้ + ฐานข้อมูลบนคลาวด์ (ลบถาวร ทุกเครื่อง)
**ล้างอะไรในเครื่อง:** (ก) ข้อมูลธุรกิจทั้งหมด เก็บพนักงาน/PIN *(แนะนำ)* (ข) เก็บข้อมูลหลัก (สินค้า+สต็อก ลูกค้า ซัพพลายเออร์ พนักงาน) ล้างเฉพาะบิล/กะ/ประวัติ (ค) ทุกอย่างเหมือนติดตั้งใหม่

- **ด่านป้องกัน:** ยืนยัน PIN → พิมพ์ "ล้างข้อมูล" → (ล้างคลาวด์) พิมพ์ชื่อโฮสต์ฐานข้อมูล + ยืนยันอีกครั้ง → ดาวน์โหลดไฟล์สำรองอัตโนมัติก่อนล้าง (ติ๊กไว้ให้) · แสดงจำนวนบิลที่ยังไม่ซิงค์ (จะหาย) และยอดหนี้ลูกค้าที่จะถูกรีเซ็ต
- **ล้างคลาวด์ก่อน ค่อยล้างเครื่อง** — ถ้าเซิร์ฟเวอร์ปฏิเสธ (ไม่ใช่เจ้าของ / ยังไม่รัน SQL) ข้อมูลในเครื่องจะไม่ถูกแตะ · ลบรูปใน Storage ด้วย · คงไว้เฉพาะบัญชีเจ้าของและ **ประวัติการใช้งาน** (เป็นหลักฐานว่าใครล้าง — บันทึก `DATA_RESET` / `SERVER_RESET` ไว้เสมอ)
- **เปลี่ยน URL ฐานข้อมูลใหม่:** ถ้ามีข้อมูลในเครื่องอยู่ ระบบเด้งหน้าต่างบังคับเลือก "ล้าง" หรือ "ยกเลิก (กลับไปใช้ฐานข้อมูลเดิม)" — ไม่ปล่อยให้ข้อมูลเก่าปนเข้าฐานข้อมูลใหม่ · พนักงานที่เก็บไว้จะถูกตัดความผูกกับฐานข้อมูลเดิม แล้วสร้างบัญชีให้บนฐานข้อมูลใหม่ตอนกด "บังคับซิงค์"
- **ไม่รองรับย้ายบิลย้อนหลังข้ามฐานข้อมูล** — การส่งบิลเก่าเข้า `create_sale` จะหักสต็อกซ้ำ จึงมีตัวเลือก (ข) แทน
- เจอและแก้ระหว่างทำ: **ขายออฟไลน์ไว้ก่อนแล้วซิงค์ครั้งแรก สต็อกถูกหักซ้ำ** (ส่งสต็อกหลังหักไปแล้ว บิลถูกหักอีกรอบ) — ตอนนี้ส่งสต็อกตั้งต้นให้ถูก
- การสร้างฐานข้อมูลใหม่ (โปรเจกต์ Supabase ใหม่): รัน `pos_schema_v3.sql` ในโปรเจกต์ใหม่ → ใส่ URL/key ใหม่ที่ตั้งค่า → เลือกล้างเครื่อง → สมัครเจ้าของร้านครั้งแรก → บังคับซิงค์

## 5) หน้าจอมือถือ + ใช้งานเหมือนแอปจริง (PWA)
**มือถือ (จอ ≤767px — เดสก์ท็อปยังเป็นหน้าเดิม):**
- แถบเมนูล่าง 4 หน้าหลัก (ตามสิทธิ์) + ปุ่ม ☰ เมนู เปิดแผ่นเลื่อนขึ้นแสดงทุกหน้า, สถานะเปิด/ปิดร้าน, ล็อกหน้าจอ, ติดตั้งแอป
- หน้าขาย: สินค้า 2 คอลัมน์ปุ่มใหญ่ · เพิ่มลงตะกร้าแล้วมี **แถบตะกร้าลอย** (จำนวน/ยอดรวม/ปุ่มชำระเงิน) · แตะแถบเพื่อดูตะกร้าเป็นแผ่นเลื่อนขึ้น · สั่นเบาๆ เมื่อเพิ่มของ/เกิด error (Android)
- หน้าต่างทั้งหมดเป็น bottom sheet · ตารางเปลี่ยนเป็นการ์ดมีป้ายกำกับ (สินค้า ลูกค้า พนักงาน รายการบิล ประวัติใช้งาน) · ปุ่ม + เพิ่ม เป็นปุ่มลอย · ช่องกรอก ≥16px (iPhone ไม่ซูมเอง) · ปุ่ม ≥44px · แป้นพิมพ์เปิดแล้วแถบล่างหลบให้
- **ปุ่มย้อนกลับของมือถือปิดหน้าต่างที่เปิดอยู่** แทนที่จะออกจากแอป
**เหมือนแอปจริง:** ติดตั้งลงหน้าจอหลัก (ไอคอน ชื่อ เต็มหน้าจอ ไม่มีแถบเบราว์เซอร์) · **เปิดใช้งาน/ขายได้แม้ไม่มีเน็ต** (Tailwind คอมไพล์ฝังในไฟล์ + service worker แคชแอปและไลบรารี) · ทางลัดกดไอคอนค้าง → ขาย/สินค้า/รายงาน · แจ้งเตือนเวอร์ชันใหม่ · ป้าย 📴 ออฟไลน์ · ขอสิทธิ์ให้เบราว์เซอร์ **ไม่ลบข้อมูลในเครื่อง** (แสดงสถานะที่ตั้งค่า → ติดตั้งเป็นแอป)
- Chromium ยืนยันว่าเข้าเกณฑ์ติดตั้งได้ (ไม่มี installability error) และทดสอบออฟไลน์ + อัปเดตเวอร์ชันผ่าน

---

## 6) การเลือกสินค้าแบบใหม่ (หน้าขาย / ใบสั่งซื้อ / ปรับสต็อก)
**ใบสั่งซื้อ (จัดซื้อ → สร้างใบสั่งซื้อ):** เลิกใช้ dropdown 2 ชั้น เปลี่ยนเป็นรายการแบบตารางสินค้า — หนึ่งแถวต่อหนึ่งขนาด แสดงคงเหลือ/ทุนล่าสุด ติ๊กเลือกได้ทันที
- ค้นหาหลายคำ (เช่น "กระดาษ 1.5"), กรองหมวดหมู่, ปุ่ม "เฉพาะใกล้หมด" (เรียงขาดมากสุดก่อน), "เลือกทั้งหมดที่แสดง", "+ เติมของใกล้หมด" คลิกเดียว
- รายการที่เลือกแก้จำนวน/ทุนได้ในแถว มียอดรวมประมาณ · การเลือกไม่หายเมื่อเปลี่ยนคำค้น
- จำนวนตั้งต้นของสินค้าใกล้หมด = เติมให้ถึง 2 เท่าของสต็อกขั้นต่ำ — **นี่เป็นกติกาที่ผมตั้งเองเป็นค่าเริ่มต้น** ไม่ได้มาจากข้อมูลการขายจริง แก้จำนวนได้ทุกแถว (ดูข้อเสนอแนะข้อ 5)
- หน้า **ปรับสต็อกมือ** ใช้ตัวเลือกแบบเดียวกัน (ค้นหา → ติ๊กเลือก) และเปลี่ยนคำ SET/DELTA เป็น "ตั้งค่าใหม่ (นับได้จริง)" / "เพิ่ม/ลดจากเดิม"

**หน้าขาย:**
- ค่าเริ่มต้นเป็น **รายการรายขนาด**: แตะแถว = เพิ่มลงตะกร้า · แถวที่อยู่ในตะกร้าเปลี่ยนเป็นปุ่ม − จำนวน + ในแถวเดียว (ไม่ต้องเปิดหน้าต่างเลือกขนาดอีกแล้ว) · หน่วยแบ่งขาย (เมตร ฯลฯ) เป็นปุ่มใต้แถว · ของหมดกดไม่ได้ · ของใกล้หมดมี ⚠️ · สลับกลับเป็นการ์ดได้ที่ปุ่ม ▦/☰ (จำค่าไว้)
- ค้นหาหลายคำ (ชื่อ/ขนาด/รหัส/บาร์โค้ด/หน่วยแบ่งขาย) · ชิปหมวดหมู่ + ⭐ ขายบ่อย (นับจากบิล 30 วันล่าสุด) · Enter เพิ่มทันทีถ้าตรงบาร์โค้ด/เหลือรายการเดียว · เดสก์ท็อปเคอร์เซอร์อยู่ที่ช่องค้นหาเสมอ (สแกนต่อเนื่อง) · มือถือแถบค้นหา+หมวดเกาะใต้หัวแอปตอนเลื่อน
- **ตะกร้า:** พิมพ์จำนวนเองได้ (ชิ้นเต็ม = จำนวนเต็ม, หน่วยแบ่งขาย = ทศนิยม 2 ตำแหน่ง) เกินสต็อกจะปรับให้เหลือเท่าที่ขายได้พร้อมแจ้ง · มีปุ่ม ✕ ลบรายการ · แสดงราคารวมรายบรรทัด
- **แก้ให้ถูกต้อง:** ราคาในตะกร้า/บิลพักเดิมเก็บ "ราคาตอนหยิบ" ถ้าแก้ราคาสินค้าทีหลังจะขายผิดราคา → ตอนชำระเงินระบบอัปเดตราคาให้ตรงปัจจุบัน แจ้งเตือน และให้ตรวจยอดอีกครั้งก่อนยืนยัน (สินค้าที่ถูกลบออกจากตะกร้าอัตโนมัติ)
- เจอและแก้ระหว่างทดสอบ: แถบหมวดหมู่ที่เลื่อนแนวนอนดันหน้ากว้างเกินจอมือถือ

---

## ข้อเสนอแนะเพื่อพัฒนาต่อ (เรียงตามคุ้มค่า/ทำได้เร็ว)
1. **ส่วนลด (ต่อบิล/ต่อรายการ)** — ฟังก์ชัน `create_sale` ฝั่งฐานข้อมูลรับ `p_discount_amount` และ `p_tax_amount` อยู่แล้ว แต่แอปส่ง 0 ตลอด เพิ่มหน้าจอกรอก + บันทึกลงประวัติการใช้งาน (ควรจำกัดสิทธิ์ให้ผู้จัดการอนุมัติส่วนลดเกินกำหนด)
2. **PromptPay QR ตามยอดเงิน** เมื่อเลือกชำระแบบโอน — ลูกค้าสแกนจ่ายได้เลย ลดการพิมพ์ยอดผิด (สร้าง QR ในแอปได้ ไม่ต้องใช้บริการภายนอก)
3. **ใบเสร็จ**: ใส่ชื่อร้าน ที่อยู่ เลขผู้เสียภาษี และพิมพ์เครื่องพิมพ์ความร้อน 58/80 มม. / แชร์เป็นรูปทาง LINE
4. **โหมดตรวจนับสต็อก** — สแกน/ติ๊กนับของจริงทีละรายการ แล้วสรุปผลต่างก่อนกดปรับทีเดียว (ตอนนี้ปรับได้ทีละรายการ)
5. **แนะนำจำนวนสั่งซื้อจากยอดขายจริง** (เฉลี่ยรายวัน × ระยะเวลารอของ) แทนกติกา "2 เท่าของสต็อกขั้นต่ำ" + ส่งใบสั่งซื้อเป็น PDF/LINE ให้ซัพพลายเออร์
6. **สำรองข้อมูลอัตโนมัติ/แจ้งเตือน** — ข้อมูลหลักอยู่ในเบราว์เซอร์ ควรมีแจ้งเตือน "ยังไม่ได้สำรองมา 7 วัน" หรือส่งไฟล์สำรองขึ้น Google Drive
7. **ราคาขายส่ง/ราคาตามกลุ่มลูกค้า** และรายงานอายุหนี้ลูกหนี้ (ค้างกี่วัน) + ใบวางบิล
8. **ตรวจสอบสิทธิ์ฝั่งเซิร์ฟเวอร์ให้ครบ** — ตอนนี้ PIN เป็นการล็อกหน้าจอ ถ้าต้องการความปลอดภัยจริงควรให้เซิร์ฟเวอร์ตรวจ PIN แล้วออกสิทธิ์ตามรอบการใช้งาน และล็อกหน้าจออัตโนมัติเมื่อไม่ได้ใช้งาน
9. พิมพ์ป้ายบาร์โค้ดให้สินค้าที่ยังไม่มีบาร์โค้ด · ค่าใช้จ่าย/เงินสดออกในลิ้นชัก (ตาราง cash_ledger รองรับแล้ว) · แจ้งเตือนสินค้าใกล้หมดตอนเปิดร้าน

---

## 7) ตรวจสอบเจาะลึกรอบล่าสุด — บั๊กที่พบใหม่และแก้แล้ว

วิธีตรวจ: เขียนสคริปต์จำลองการใช้งานแบบ "จ้องจับผิด" (double-click รัว, เปิดหลายแท็บ, ไม่มี https, เลขบิลชนกัน ฯลฯ) แทนการอ่านโค้ดเฉยๆ แล้วพิสูจน์ผลจริงก่อน/หลังแก้ทุกข้อ

| # | บั๊ก | ผลกระทบ | วิธีแก้ |
|---|---|---|---|
| 1 | ตั้ง/ล็อกอินด้วย PIN พังทั้งระบบถ้าเปิดแอปผ่าน **http** (ไม่ใช่ https) เช่น `http://192.168.1.10` บนเครือข่ายภายในร้าน | `crypto.subtle` (ใช้แฮช PIN) มีให้ใช้เฉพาะ "secure context" เท่านั้น — ร้านที่ไม่ได้ทำโดเมน/ใบรับรองเอง มักเปิดผ่าน IP ภายในแบบ http ธรรมดา ทำให้ตั้ง PIN พนักงานไม่ได้เลยตั้งแต่ต้น | เพิ่มทางสำรองด้วย SHA-1 (โค้ดมีอยู่แล้วในไฟล์สำหรับสร้าง UUID) ใช้เฉพาะกรณีไม่มี `crypto.subtle` — PIN เป็นแค่ตัวล็อกหน้าจอตามที่ตั้งใจไว้แต่แรก ไม่ใช่การเข้ารหัสความปลอดภัยของข้อมูล |
| 2 | ปัดเศษราคา/สต็อกได้ `NaN` เมื่อเจอเลขทศนิยมเพี้ยนเล็กน้อยจากการคำนวณ (เช่น 0.3−0.1−0.2 = -0.00000000000000006) | `NaN` เทียบค่ามากกว่า/น้อยกว่าอะไรก็ได้ `false` เสมอ — ทำให้ **การ์ดกันขายเกินสต็อกหลุดเงียบๆ** โดยไม่มี error ให้เห็น | ค่าที่เล็กจิ๋วขนาดนี้ปัดเป็น 0 ตรงๆ ก่อนเข้าสูตรเดิม (ปัด 2/4 ตำแหน่งแล้วก็ได้ 0 อยู่แล้ว จึงไม่กระทบตัวเลขปกติ) |
| 3 | กดปุ่มซ้ำเร็วๆ (นิ้วโป้งกดสองทีบนจอสัมผัส) ที่ปุ่ม **รับชำระหนี้ / ปรับสต็อก / ชำระเงิน** | ส่งคำสั่งซ้ำสองครั้ง หักหนี้ลูกค้า/ปรับสต็อกซ้ำสองเท่าโดยไม่ตั้งใจ (พิสูจน์แล้ว: กดพร้อมกัน 2 ครั้ง หนี้ที่ควรลด 100 บาท ลดไป 200 บาทจริง) | ใส่ตัวกันการทำงานซ้ำระหว่างรอผลจากเซิร์ฟเวอร์ (เหมือนที่มีอยู่แล้วในปุ่มคืนสินค้า) ครบทุกปุ่มที่แตะเงิน/สต็อก |
| 4 | แนบรูปสินค้าตอนออฟไลน์ (รูปถ่ายจากกล้องมือถือ มักเกิน 3-5MB) | เก็บเป็น data: URL ขนาดเท่าไฟล์ต้นฉบับตรงๆ ปนอยู่ในก้อนข้อมูลเดียวกับสินค้า/บิลทั้งหมด แนบรูปแค่ 1-2 รูปก็ทำให้พื้นที่เก็บข้อมูลในเครื่องเต็มจนบันทึกอะไรต่อไม่ได้เลย | ย่อ/บีบอัดรูปก่อนเก็บเสมอ (ทดสอบจริง: 5.3MB เหลือ ~360KB) ทั้งตอนแนบตอนออฟไลน์และตอนอัปโหลดขึ้น Supabase Storage |
| 5 | คืนสินค้าในกะถัดไป (คนละวัน/คนละกะกับตอนขาย) | เงินสดที่จ่ายคืนถูกหักออกจาก **กะที่ขาย (ปิดไปแล้ว)** แทนกะที่จ่ายเงินคืนจริง — กะที่จ่ายเงินจริงจะปิดแล้วเงินสดขาดโดยหาสาเหตุไม่เจอ | ผูกยอดคืนกับกะที่กดคืนจริง ไม่ใช่กะที่ขาย และบังคับต้องเปิดร้านก่อนจึงจะคืนสินค้าได้ (กันเงินคืนที่ไม่ผูกกับกะไหนเลย) |
| 6 | เลขบิลชนกันโดยบังเอิญกับบิลของ "เครื่องอื่น" บนคลาวด์ (คนละยอดเงิน) | เดิมเจอ error ชนกัน (23505) แล้ว **ถือว่าซิงค์สำเร็จทันทีโดยไม่เช็คยอด** ทำให้ยอดขายจริงหายไปเงียบๆ (ถูกทำเครื่องหมายว่าสำเร็จทั้งที่จริงเป็นบิลของคนอื่น) | เช็คยอดก่อนเชื่อ ถ้าไม่ตรงให้เปลี่ยนเลขบิลแล้วส่งใหม่อัตโนมัติ (จำกัด 3 ครั้ง) พร้อมบันทึกลงประวัติการใช้งาน |
| 7 | เปิดแอปพร้อมกัน 2 แท็บ/หน้าต่างบนเครื่องเดียวกัน (เช่น แอปที่ติดตั้งไว้ค้างอยู่ + เปิดเบราว์เซอร์อีกแท็บดูรายงาน) | แท็บที่บันทึกทีหลังเขียนทับข้อมูลด้วยสำเนาเก่าของตัวเอง ทำให้การขาย/แก้ไขที่ทำในอีกแท็บหายไปเงียบๆ | ฟังการเปลี่ยนแปลงจากแท็บอื่น (เบราว์เซอร์แจ้งให้อัตโนมัติ) แล้วโหลดข้อมูลล่าสุดมาแทนที่ก่อนเรนเดอร์ใหม่ (ยังคงผู้ใช้ที่ล็อกอินของแท็บตัวเองไว้) |

ตรวจแล้วไม่พบปัญหา: ข้อความอันตรายในหน้า Log (เช่น ชื่อสินค้าที่มีโค้ด HTML) ถูก escape ครบ ไม่รันสคริปต์ได้

### วิจารณ์ + ข้อจำกัดที่ยังไม่แก้ในรอบนี้ (สถาปัตยกรรม ไม่ใช่บั๊ก)
- **ข้อมูลทั้งร้านเก็บใน localStorage ก้อนเดียว (`pos2_db`)** ซึ่งมีเพดานเดียวกับที่เบราว์เซอร์ให้ทั้งโดเมน (ทั่วไป ~5-10MB) และการบันทึกทุกครั้ง (`persist()`) คือ serialize ทั้งก้อนแล้วเขียนทีเดียว (บล็อกหน้าจอสั้นๆ) ทดสอบจริง: ที่ขนาด ~5MB (บิลประมาณ 5,500 ใบ) การบันทึกแต่ละครั้งใช้เวลา ~150ms และร้านที่ขาย 100 บิล/วันจะเต็มพื้นที่ในราว 2 เดือน — เพิ่ม **การเตือนล่วงหน้า** เมื่อข้อมูลเกิน ~3.5MB ให้สำรองข้อมูลแล้วในรอบนี้ แต่การแก้ที่ต้นเหตุจริงๆ (ย้ายไปใช้ IndexedDB ซึ่งไม่มีเพดานขนาดนี้และอ่าน/เขียนแบบ async ไม่บล็อกหน้าจอ) เป็นงานใหญ่ที่ควรทำแยกเป็นโปรเจกต์ต่างหาก ไม่เหมาะรีบทำแทรกในรอบแก้บั๊กนี้ (เสี่ยงทำของเดิมพังมากกว่าประโยชน์ที่ได้ในเวลาอันจำกัด)
- ทดสอบกับสินค้า 5,000 รายการ (10,000 ขนาด): หน้าจอยังตอบสนองเร็วอยู่ (เรนเดอร์หน้าแรก, ค้นหา, เพิ่มลงตะกร้า ล้วนไม่ถึง 0.15 วินาที) ยังไม่ใช่ปัญหาในขนาดร้านค้าทั่วไป

---

## พฤติกรรมที่เปลี่ยน (ควรแจ้งพนักงาน)
- ขนาดสินค้าที่ **ซิงค์แล้วช่องสต็อกในฟอร์มแก้ไขจะถูกล็อก** ต้องปรับที่ จัดซื้อ → ปรับสต็อกมือ (ต้องใส่เหตุผล) — กันสต็อกถูกเขียนทับข้ามเครื่อง
- ต้อง **เปิดร้าน (เปิดกะ) ก่อนขาย** และตอนปิดร้านต้องกรอกเงินสดที่นับได้ ระบบบันทึกส่วนต่างให้
- เครื่องที่ยังไม่มีใครตั้ง PIN ถือว่าผู้ใช้คือเจ้าของ (เพื่อให้ตั้งค่าครั้งแรกได้) — ตั้ง PIN แล้วจึงเริ่มบังคับล็อกอิน

## ข้อจำกัด / สิ่งที่ยังไม่ได้ทดสอบจริง
- **ก่อนล้างฐานข้อมูลจริงครั้งแรก ทดลองกับโปรเจกต์ Supabase ทดลองก่อน** (ฟังก์ชัน `factory_reset_cloud_data` เป็นของเดิมในสคีมา ผมเพิ่มการบันทึกประวัติ แต่ยังไม่ได้รันกับ PostgreSQL จริง)
- iPhone: ติดตั้งผ่าน Safari เท่านั้น และแนะนำให้ติดตั้งลงหน้าจอโฮม (Safari ปกติอาจล้างข้อมูลเว็บที่ไม่ได้เปิดนานราว 7 วัน — แอปที่ติดตั้งแล้วไม่โดน) ยังควรสำรองข้อมูลสม่ำเสมออยู่ดี
- **SQL ยังไม่ได้รันกับ PostgreSQL จริง** (เครื่องทดสอบไม่มีฐานข้อมูล) ผมอ่านทบทวนแล้วและเทียบกับโครงสร้างเดิม แต่ควรรันกับโปรเจกต์ทดลอง/หลังสำรองก่อน หากมี error ส่งข้อความมาให้ดูได้
- ฝั่งแอปทดสอบอัตโนมัติ 101 กรณีบน Chromium (จำลองหน้าจอโทรศัพท์ด้วย) — รวมชุดทดสอบใหม่ 9 กรณีที่ล็อกบั๊กรอบนี้ไว้ไม่ให้กลับมาอีก โดยใช้ Supabase จำลองที่ทำตามกฎของสคีมา (unique, FK, RLS, จำกัด 1,000 แถว, RPC) — **ยังไม่ได้ทดสอบกับ Supabase จริง (รวมปุ่มล้างฐานข้อมูลบนคลาวด์), มือถือเครื่องจริง (iPhone Safari / Android Chrome), กล้องสแกนบาร์โค้ด, นำเข้า/ส่งออก Excel, การพิมพ์ใบเสร็จ** (ไลบรารีเหล่านี้โหลดจาก CDN ซึ่งเครื่องทดสอบเข้าไม่ได้)
- **PIN 4 หลักคือการล็อกหน้าจอ ไม่ใช่ความปลอดภัยระดับข้อมูล** — ค่าแฮชอ่านได้จากไฟล์สำรองและผู้จัดการอ่านจากตาราง `app_users` ได้ และเดาย้อนได้ในไม่กี่วินาที การป้องกันจริงอยู่ที่บัญชี Supabase Auth + RLS/RPC ฝั่งเซิร์ฟเวอร์ ไฟล์สำรองมีรหัสผ่านบัญชีพนักงานที่ระบบสร้างให้ ควรเก็บให้ปลอดภัย
- ข้อมูลสินค้าหลายเครื่องเป็นแบบ "ใครซิงค์ทีหลังชนะ" (สต็อกที่ซิงค์แล้วเซิร์ฟเวอร์เป็นตัวตัดสิน) — ถ้าหลายเครื่องแก้ราคาพร้อมกันควรดึงข้อมูลก่อนแก้
